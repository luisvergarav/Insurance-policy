apiVersion: v1
kind: Service
metadata:
  name: {{ include "Insurance-policy.fullname" . }}
  labels:
    {{- include "Insurance-policy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "Insurance-policy.selectorLabels" . | nindent 4 }}
